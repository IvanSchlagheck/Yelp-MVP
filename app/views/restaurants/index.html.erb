<div class= "mx-3">
  <ul>
    <% @restaurants.each do |restaurant| %>
    <li class= "my-3"><%= link_to restaurant.name, restaurant_path(restaurant) %> - <%= link_to "Delete", restaurant_path(restaurant), method: :delete, data: { confirm: "Are you sure?" } %></li>
    <% end %>
  </ul>
  <br>
  <%= link_to "Create a new Restaurant", new_restaurant_path %>
</div>
